{
  "name": "stealth_recon",
  "description": "Low-and-slow stealth recon: fragmented SYN scan with decoy IPs to avoid IDS detection.",
  "author": "james-linux",
  "version": "1.0",
  "requires_root": true,
  "steps": [
    {
      "id": "stealth_syn",
      "action": "nmap.scan",
      "params": {
        "target": "{{target}}",
        "flags": "-sS -T2 -f --data-length 24 -D RND:5 --max-retries 1",
        "ports": "{{ports}}",
        "sudo": true
      },
      "description": "Fragmented SYN scan at paranoid speed with random decoys"
    },
    {
      "id": "service_detect",
      "action": "nmap.scan",
      "params": {
        "target": "{{target}}",
        "flags": "-sV -T2 --version-intensity 3",
        "ports": "{{ports}}",
        "sudo": true
      },
      "description": "Low-intensity service version detection on discovered ports"
    }
  ],
  "category": "recon"
}
