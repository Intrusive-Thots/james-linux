{
  "name": "ssl_audit",
  "description": "Complete SSL/TLS security audit: sslscan cipher analysis + nmap SSL scripts for vulnerabilities.",
  "author": "james-linux",
  "version": "1.0",
  "requires_root": false,
  "steps": [
    {
      "id": "sslscan",
      "action": "sslscan.scan",
      "params": {
        "target": "{{target}}"
      },
      "description": "SSLScan cipher suite and protocol analysis"
    },
    {
      "id": "nmap_ssl",
      "action": "nmap.scan",
      "params": {
        "target": "{{target}}",
        "flags": "--script ssl-enum-ciphers,ssl-cert,ssl-heartbleed,ssl-poodle,ssl-dh-params",
        "ports": "443"
      },
      "description": "Nmap SSL vulnerability scripts (Heartbleed, POODLE, weak DH)"
    }
  ],
  "category": "web"
}
